% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_tools.R
\name{dataset_variance_structure_tool}
\alias{dataset_variance_structure_tool}
\title{Dataset tool: how much dataset variance aligns with each metadata covariate}
\usage{
dataset_variance_structure_tool(ctx)
}
\arguments{
\item{ctx}{A dataset tool context list: \code{list(ms, params, module_method)},
as built by \code{\link[=run_dataset_context]{run_dataset_context()}}. \code{ctx$params$covariates} (required)
is a character vector of pseudo-bulk metadata columns to regress each PC
against. \code{ctx$params$condition_col} (required) names the biological
covariate among \code{covariates}; every other entry is treated as
\code{'technical'} for the confounding check unless
\code{ctx$params$technical_covariates} overrides that. \code{ctx$params$n_components}
(default \code{10}) is the number of principal components to compute,
clamped to what the pseudo-bulk matrix can support.}
}
\value{
A \code{dataset_fragment} of type \code{'variance_structure'}, or \code{NULL} if
\code{\link[=pseudobulk_view]{pseudobulk_view()}} can't resolve a pseudo-bulk view for \code{ctx$ms} -- a
graceful skip, not an error.
}
\description{
The trust prior for every downstream \code{cross_condition_delta}: runs PCA on
the pseudo-bulk expression matrix, then regresses each principal component
against each declared metadata covariate (\code{lm(pc_scores ~ covariate)}),
reporting \code{R2}/\code{adj_R2}/\code{fdr} per covariate x PC pair. Lets the synthesis
prompt tell whether a module's cross-condition signal rides on real
biology or on a technical axis (batch, sequencing depth) that happens to
dominate the same principal component. Ported from
\code{sample_code/pseudobulk_functions.R} \code{PseudobulkPCA()} / \code{PCRegression()}
(multi-covariate branch); see \code{\link[=.pseudobulk_pca]{.pseudobulk_pca()}} / \code{\link[=.pc_regression]{.pc_regression()}}.
}
\examples{
\dontrun{
pb_ms <- pseudobulk_ModuleSet(pb_counts, list(module_a = c('GENE1', 'GENE2')), pb_meta)
dataset_variance_structure_tool(list(
    ms = pb_ms, params = list(covariates = c('condition', 'batch'), condition_col = 'condition')
))
}
}
