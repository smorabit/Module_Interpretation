% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_tools.R
\name{dataset_baseline_expression_tool}
\alias{dataset_baseline_expression_tool}
\title{Dataset tool: dataset-wide baseline expression and housekeeping signal}
\usage{
dataset_baseline_expression_tool(ctx)
}
\arguments{
\item{ctx}{A dataset tool context list: \code{list(ms, params, module_method)},
as built by \code{\link[=run_dataset_context]{run_dataset_context()}}. \code{ctx$params$top_n} (default \code{15})
is the number of globally dominant genes (by mean expression) to keep in
\code{result} and \code{top_findings} -- this list doubles as the ubiquitous-gene
set synthesis can cross-check a module's hub genes against.}
}
\value{
A \code{dataset_fragment} of type \code{'baseline_expression'}, or \code{NULL} if
\code{ctx$ms} lacks the \code{expression} capability (see \code{\link[=capabilities]{capabilities()}}) -- a
graceful skip, not an error.
}
\description{
Distinguishes specific per-module biology from dataset-wide ambient /
housekeeping signal: mean expression, detection rate, and CV per gene
(\code{expression(ms)}, full matrix, never returned), the top-N globally
dominant genes by mean expression, ribosomal (\verb{^RP[LS]}) / mitochondrial
(\verb{^MT-}) mass, and a depth (\code{nCount}) distribution summary. Touches only
the \code{ModuleSet} adapter contract (\code{\link[=expression]{expression()}}, \code{\link[=counts]{counts()}},
\code{\link[=has_capability]{has_capability()}}, \code{\link[=pkg_versions]{pkg_versions()}}). Does \strong{not} set the per-module
\code{hub_genes_are_housekeeping} caveat -- that is a judgment about one
module's hub genes, made by synthesis after reading the exposed dominant-
gene list, not by this tool.
}
\examples{
ms <- llegir_example_moduleset()
dataset_baseline_expression_tool(list(ms = ms, params = list()))
}
